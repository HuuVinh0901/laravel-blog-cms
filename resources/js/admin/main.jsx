import React from 'react';
import ReactDOM from 'react-dom/client';

const AdminApp = () => {
    return <h1>Hello from Admin React App!</h1>;
};

ReactDOM.createRoot(document.getElementById('admin')).render(<AdminApp />);
